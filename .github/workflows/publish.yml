name: Publish images to GHCR (multi-arch)

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  ORG: privacyx-org
  PLATFORMS: linux/amd64,linux/arm64

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU (for cross-arch)
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from tag
        id: vars
        run: |
          REF="${GITHUB_REF##*/}"
          echo "version=${REF}" >> $GITHUB_OUTPUT

      # GATEWAY
      - name: Build & push gateway (multi-arch)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.gateway
          platforms: ${{ env.PLATFORMS }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.ORG }}/px-gateway:${{ steps.vars.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.ORG }}/px-gateway:latest

      # SCHEDULER
      - name: Build & push scheduler (multi-arch)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.scheduler
          platforms: ${{ env.PLATFORMS }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.ORG }}/px-scheduler:${{ steps.vars.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.ORG }}/px-scheduler:latest

      # MINER
      - name: Build & push miner (multi-arch)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.miner
          platforms: ${{ env.PLATFORMS }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.ORG }}/px-miner:${{ steps.vars.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.ORG }}/px-miner:latest

